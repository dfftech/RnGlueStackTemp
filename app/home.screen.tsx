import HomePage from '@/modules/home/home.page';
import React from 'react';
import { ScrollView } from 'react-native';

export default function HomeScreen() {
	return (
		<ScrollView> <HomePage /> </ScrollView>
	);
}

